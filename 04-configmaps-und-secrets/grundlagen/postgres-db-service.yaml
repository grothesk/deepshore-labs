apiVersion: v1
kind: Service
metadata:
  labels:
    app: postgres-db
  name: postgres-db
spec:
  ports:
  - port: 5432
    protocol: TCP
    targetPort: 5432
  selector:
    app: postgres-db
  type: ClusterIP
